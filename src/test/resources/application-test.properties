# PostgreSQL: URL de conex�o
spring.datasource.url=jdbc:postgresql://localhost:5432/study1
spring.datasource.username=postgres
spring.datasource.password=matheuseduardo
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate: cria as tabelas no in�cio e apaga no fim dos testes
spring.jpa.hibernate.ddl-auto=create-drop

# Dialeto do PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mostrar SQL no console (opcional)
#spring.jpa.show-sql=true
