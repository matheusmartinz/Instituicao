# PostgreSQL: URL de conexão
spring.datasource.url=jdbc:postgresql://localhost:5432/study1
spring.datasource.username=postgres
spring.datasource.password=matheusak
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate: cria as tabelas no início e apaga no fim dos testes
spring.jpa.hibernate.ddl-auto=create-drop

# Dialeto do PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mostrar SQL no console (opcional)
spring.jpa.show-sql=true
